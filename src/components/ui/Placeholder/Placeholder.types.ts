// Interfaces and types from component Placeholder

// Component Props
export interface PlaceholderProps {
  className?: string;
  children?: React.ReactNode;
  variant?: "default" | "alt";
  pulse?: boolean;
  shine?: boolean;
  rounded?: boolean;
}
